# 🔧 แก้ไขปัญหา max_checkins_per_user ไม่ทำงาน

## ปัญหาที่พบ

เมื่อเปลี่ยน `max_checkins_per_user` จาก 1 เป็น 4 ผู้ใช้ยังไม่สามารถลงทะเบียนวันใหม่ได้ เนื่องจากระบบขึ้นว่า **"คุณได้ลงทะเบียนกิจกรรมนี้แล้ว"**

## สาเหตุ

มี **2 จุดที่มีปัญหา**:

### 1. การนับจำนวน check-ins ผิด (แก้แล้ว)

**ไฟล์:** `src/services/scheduler_service.py` และ `src/crud/event_participation_crud.py`

**ปัญหา:** โค้ดเดิมนับเฉพาะสถานะ `CHECKED_IN` และ `COMPLETED` เท่านั้น ไม่นับรวมรหัสที่สร้างไว้แล้วแต่ยังไม่ได้เช็คอิน (สถานะ `JOINED` หรือ `EXPIRED`)

**วิธีแก้:** เปลี่ยนให้นับทุกสถานะที่ไม่ใช่ `CANCELLED`

```python
# Before (ผิด)
ParticipationStatus.status.in_([
    ParticipationStatus.CHECKED_IN,
    ParticipationStatus.COMPLETED
])

# After (ถูกต้อง)
ParticipationStatus.status != ParticipationStatus.CANCELLED
```

### 2. ❌ ฟังก์ชัน pre_register บล็อคไม่ให้ลงทะเบียนซ้ำ (ปัญหาหลัก)

**ไฟล์:** `src/crud/event_participation_crud.py` (ฟังก์ชัน `pre_register_for_multi_day_event`)

**ปัญหา:** โค้ดเดิมตรวจสอบว่าถ้ามี participation อันใดอันหนึ่งแล้ว ก็บล็อคไม่ให้ลงทะเบียนอีกเลย โดยไม่สนใจ `max_checkins_per_user`

```python
# โค้ดเดิม (ผิด)
existing = await db.execute(
    select(EventParticipation)
    .where(
        and_(
            EventParticipation.user_id == user_id,
            EventParticipation.event_id == event_id,
            EventParticipation.status != ParticipationStatus.CANCELLED
        )
    )
    .limit(1)
)

if existing.scalar_one_or_none():
    raise HTTPException(
        status_code=status.HTTP_400_BAD_REQUEST,
        detail="คุณได้ลงทะเบียนกิจกรรมนี้แล้ว"  # ❌ บล็อคทันที!
    )
```

**วิธีแก้:** เปลี่ยนให้ตรวจสอบ 2 เงื่อนไข:
1. ✅ วันนี้ลงทะเบียนแล้วหรือยัง (1 วัน = 1 รหัส)
2. ✅ จำนวนทั้งหมดครบตาม `max_checkins_per_user` แล้วหรือยัง

```python
# โค้ดใหม่ (ถูกต้อง)
# 1. เช็ควันนี้
today_check = await db.execute(
    select(EventParticipation)
    .where(
        and_(
            EventParticipation.user_id == user_id,
            EventParticipation.event_id == event_id,
            EventParticipation.checkin_date == today,
            EventParticipation.status != ParticipationStatus.CANCELLED
        )
    )
)

if today_check.scalar_one_or_none():
    raise HTTPException(
        status_code=status.HTTP_400_BAD_REQUEST,
        detail=f"คุณได้ลงทะเบียนวันนี้แล้ว (วันที่ {today})"
    )

# 2. เช็คจำนวนทั้งหมด
if event.max_checkins_per_user:
    total_count_result = await db.execute(
        select(func.count(EventParticipation.id))
        .where(
            EventParticipation.user_id == user_id,
            EventParticipation.event_id == event_id,
            EventParticipation.status != ParticipationStatus.CANCELLED
        )
    )
    total_count = total_count_result.scalar() or 0

    if total_count >= event.max_checkins_per_user:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=f"คุณลงทะเบียนครบ {event.max_checkins_per_user} ครั้งแล้ว"
        )
```

## ผลลัพธ์

### Before (ผิด)
- ลงทะเบียนวันแรก → ได้รหัส 1 รหัส
- พยายามลงทะเบียนวันถัดไป → ❌ "คุณได้ลงทะเบียนกิจกรรมนี้แล้ว"
- **ไม่สนใจว่า `max_checkins_per_user = 4`**

### After (ถูกต้อง)
- ลงทะเบียนวันที่ 1 → ได้รหัส (1/4)
- ลงทะเบียนวันที่ 2 → ได้รหัส (2/4)
- ลงทะเบียนวันที่ 3 → ได้รหัส (3/4)
- ลงทะเบียนวันที่ 4 → ได้รหัส (4/4)
- ลงทะเบียนวันที่ 5 → ❌ "คุณลงทะเบียนครบ 4 ครั้งแล้ว"

## การทดสอบ

รันสคริปต์ทดสอบ:

```bash
python test_pre_register_fix.py
```

สคริปต์จะแสดง:
- จำนวนรหัสที่สร้างไปแล้ว
- จำนวนที่เหลือ
- สถานะแต่ละวัน
- ว่าสามารถลงทะเบียนวันนี้ได้หรือไม่

## ไฟล์ที่แก้ไข

1. ✅ `src/services/scheduler_service.py` (บรรทัด 96-112)
2. ✅ `src/crud/event_participation_crud.py` (บรรทัด 695-715, 985-1030)

## หมายเหตุ

- ผู้ใช้สามารถลงทะเบียนได้ **1 ครั้งต่อวัน** (ไม่ได้หลายครั้งในวันเดียว)
- จำนวนครั้งทั้งหมดถูกจำกัดด้วย `max_checkins_per_user`
- Scheduler จะสร้างรหัสอัตโนมัติทุกวัน จนครบจำนวน
- ถ้าไม่มี `max_checkins_per_user` (null) = ไม่จำกัดจำนวนครั้ง
