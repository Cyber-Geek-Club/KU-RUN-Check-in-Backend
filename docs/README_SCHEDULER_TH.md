# 🔓🔒 ระบบปลดล็อคและล็อครหัสอัตโนมัติ

## คำอธิบายระบบ

ระบบนี้จะจัดการรหัสเข้าร่วมกิจกรรมแบบหลายวันอัตโนมัติ โดยมี 2 ฟีเจอร์หลัก:

### 🔓 Auto-Unlock (ปลดล็อคอัตโนมัติ)
- **เวลาทำงาน**: ทุกวันเวลา 00:00 น.
- **ทำอะไร**: สร้างรหัสใหม่ให้ผู้ใช้ที่ลงทะเบียนล่วงหน้าไว้แล้ว
- **ผลลัพธ์**: ผู้ใช้จะได้รหัสใหม่ทุกวันโดยอัตโนมัติ ไม่ต้องมากดลงทะเบียนเอง

### 🔒 Auto-Lock (ล็อครหัสอัตโนมัติ)
- **เวลาทำงาน**: ทุกวันเวลา 23:59 น.
- **ทำอะไร**: ทำให้รหัสที่ไม่ได้ใช้หมดอายุ
- **ผลลัพธ์**: รหัสที่ไม่ได้ check-in จะเปลี่ยนสถานะเป็น "expired"

## วิธีการใช้งาน

### สำหรับผู้เข้าร่วม

1. **ลงทะเบียนล่วงหน้า** (ครั้งเดียวสำหรับทั้งกิจกรรม)
   ```
   POST /api/participations/pre-register/{event_id}
   ```
   
2. **ดูรหัสของวันนี้**
   ```
   GET /api/participations/pre-register-status/{event_id}
   ```
   
3. **นำรหัสไป check-in** กับ staff
   - รหัสจะใช้ได้เฉพาะวันนั้น
   - หมดอายุเที่ยงคืน

### สำหรับ Staff

ใช้วิธีเดิม:
```
POST /api/participations/check-in-daily
{
  "join_code": "12345"
}
```

## ตัวอย่างการทำงาน

```
📅 วันที่ 1 พ.ย.:
   - ผู้ใช้ลงทะเบียนล่วงหน้า → ได้รหัส "12345"
   - รหัสนี้ใช้ได้จนถึง 23:59 น.

📅 วันที่ 2 พ.ย. เวลา 00:00:
   - ระบบสร้างรหัสใหม่ "67890" อัตโนมัติ
   - ผู้ใช้ไม่ต้องทำอะไร แค่เข้าแอปดูรหัส

📅 วันที่ 2 พ.ย. เวลา 23:59:
   - ถ้ารหัส "67890" ไม่ได้ใช้ → เปลี่ยนเป็น "expired"
   - ถ้าใช้แล้ว → ยังคงสถานะ "checked_in"

📅 วันที่ 3 พ.ย. เวลา 00:00:
   - สร้างรหัสวันที่ 3 อัตโนมัติ
   - วนซ้ำไปเรื่อยๆ จนจบกิจกรรม
```

## ข้อดีของระบบ

✅ **สะดวก**: ลงทะเบียนครั้งเดียว ได้รหัสทุกวัน  
✅ **ปลอดภัย**: รหัสหมดอายุอัตโนมัติ ไม่มีใครเอาไปใช้ย้อนหลัง  
✅ **ไม่พลาด**: ไม่ต้องกังวลว่าลืมลงทะเบียนวันไหน  
✅ **ควบคุมได้**: ยกเลิกได้ทุกเมื่อ

## ข้อควรระวัง

⚠️ **รหัสหมดอายุทุกวัน**: ต้อง check-in ภายในวันนั้น  
⚠️ **ลงทะเบียนล่วงหน้า**: ใช้ได้เฉพาะกิจกรรมแบบหลายวัน (multi-day)  
⚠️ **ยกเลิกแล้วต้องลงใหม่**: ยกเลิกแล้วต้องลงทะเบียนใหม่ถ้าจะเข้าร่วมต่อ

## การทดสอบ

รันคำสั่ง:
```bash
python test_scheduler.py
```

เพื่อทดสอบว่าระบบทำงานได้หรือไม่
